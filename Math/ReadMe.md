to run, use 
```
source venv/bin/activate.sh
pip install jupterlab
jupyter-lab
````

Note: works in linux, on windows im using wsl and vscode 
```` 
.venv/Scripts/Activate.ps1
````
for windows use the above command is powershell to install more dependancies/libraries
otherwise follow below direections to use jupyter-lab in vscode


add jupyter notbook extension to vs code
add python to vs code

select the kernel from the .venv in the root/top directory of this repository
